<%= form_with(model: arteicle, local: true) do |form| %>
  <% if arteicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(arteicle.errors.count, "error") %> prohibited this arteicle from being saved:</h2>

      <ul>
        <% arteicle.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
